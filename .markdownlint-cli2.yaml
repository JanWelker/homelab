config:
  MD013: false
  # Card grids on the hub pages need a wrapping <div class="grid cards">;
  # the theme has no Markdown syntax for them.
  MD033:
    allowed_elements: ["div"]
  MD004:
    style: dash
