---
global:
  domain: argo.infra.k8s.wlkr.ch
  # Would union with the Cilium policy and allow the server every ingress.
  networkPolicy:
    create: false
  # Pod-level; the config audit keys on it (KSV-0118).
  securityContext:
    runAsNonRoot: true
    seccompProfile:
      type: RuntimeDefault

redis-ha:
  enabled: true
  # Ahead of the chart's frozen tag; see docs/platform/argocd.md#configuration
  image:
    # renovate: datasource=docker depName=ecr-public.aws.com/docker/library/redis versioning=docker
    tag: 8.10.1-alpine
  redis:
    resources:
      requests:
        cpu: 10m
        memory: 48Mi
      limits:
        memory: 128Mi
  sentinel:
    resources:
      requests:
        cpu: 10m
        memory: 32Mi
      limits:
        memory: 128Mi
  haproxy:
    # Drop once argo-cd vendors redis-ha >= 4.39.1 (DandyDeveloper/charts#424).
    image:
      # renovate: datasource=docker depName=ecr-public.aws.com/docker/library/haproxy versioning=docker
      tag: 3.4.4-alpine
    # 3 pods, hard anti-affinity, 3 nodes; see docs/platform/argocd.md#configuration
    deploymentStrategy:
      type: RollingUpdate
      rollingUpdate:
        maxSurge: 0
        maxUnavailable: 1
    resources:
      requests:
        cpu: 25m
        memory: 128Mi
      limits:
        memory: 320Mi
  splitBrainDetection:
    resources:
      requests:
        cpu: 10m
        memory: 32Mi
      limits:
        memory: 128Mi

# controller has no resources yet: it grows with managed resources.
controller:
  replicas: 1
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true

server:
  replicas: 3
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
  extraArgs:
    - --insecure
  resources:
    requests:
      cpu: 25m
      memory: 96Mi
    limits:
      memory: 256Mi

repoServer:
  replicas: 3
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
  resources:
    requests:
      cpu: 50m
      memory: 192Mi
    limits:
      memory: 512Mi

applicationSet:
  replicas: 2
  metrics:
    enabled: true
    serviceMonitor:
      enabled: true
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
    limits:
      memory: 192Mi

dex:
  resources:
    requests:
      cpu: 10m
      memory: 48Mi
    limits:
      memory: 128Mi

notifications:
  resources:
    requests:
      cpu: 10m
      memory: 64Mi
    limits:
      memory: 128Mi

configs:
  cm:
    url: https://argo.infra.k8s.wlkr.ch

    # Must not exceed the ApplicationSets' requeueAfterSeconds; see docs/platform/argocd.md
    timeout.reconciliation: 60s

    # Break-glass if Authentik is down: see docs/platform/argocd.md
    admin.enabled: "false"

    oidc.config: |
      name: Authentik
      issuer: https://auth.k8s.wlkr.ch/application/o/argocd/
      clientID: $argocd-oidc:client-id
      clientSecret: $argocd-oidc:client-secret
      requestedScopes:
        - openid
        - profile
        - email
      requestedIDTokenClaims:
        groups:
          essential: true

    resource.exclusions: |
      - apiGroups:
        - actions.github.com
        kinds:
        - AutoscalingListener
        - EphemeralRunnerSet
        - EphemeralRunner
        clusters:
        - "*"
    # Restores Application health (dropped in 1.8), so parents report children.
    resource.customizations.health.argoproj.io_Application: |
      hs = {}
      hs.status = "Progressing"
      hs.message = ""
      if obj.status ~= nil then
        if obj.status.health ~= nil then
          hs.status = obj.status.health.status
          if obj.status.health.message ~= nil then
            hs.message = obj.status.health.message
          end
        end
      end
      return hs
    resource.customizations: |
      gateway.networking.k8s.io/HTTPRoute:
        ignoreDifferences: |
          jsonPointers:
          - /spec/parentRefs/0/group
          - /spec/parentRefs/0/kind
          - /spec/rules/0/backendRefs/0/group
          - /spec/rules/0/backendRefs/0/kind
          - /spec/rules/0/backendRefs/0/weight
          - /spec/rules/0/matches

  rbac:
    # No access without a matching group, not read-only-everything.
    policy.default: ""
    policy.csv: |
      g, argocd-admins, role:admin
      g, argocd-viewers, role:readonly
    scopes: "[groups, email]"
