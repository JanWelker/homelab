---
name: Lint YAML
permissions:
  contents: read

"on":
  push:
    branches:
      - "main"
    paths:
      - "**/*.yaml"
      - ".yamllint"
  pull_request:
    paths:
      - "**/*.yaml"
      - ".yamllint"
  workflow_dispatch:

jobs:
  yaml-lint:
    name: YAML
    runs-on: ubuntu-latest
    container: python:3.14-slim@sha256:caaf356f40667c496d405780745b9ac25771c189a51dfcc42430d531ea09f8a2
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Install uv
        uses: astral-sh/setup-uv@c18668ad3cf93ea998bef934396af7bb5c839dc7 # v10.2.0
        with:
          version: "0.12.18"

      - name: Install the project
        run: uv sync --locked --all-extras --dev

      # A directory, not a glob: under bash `**` is `*`, so `**/*.yaml` named
      # exactly one file, which .yamllint ignores, and this job passed on
      # anything. The ignore list in .yamllint keeps .venv/ and site/ out.
      - name: Run yamllint
        run: uv run yamllint -f github .
